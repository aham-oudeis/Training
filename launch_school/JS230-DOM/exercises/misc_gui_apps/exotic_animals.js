'use strict';

document.addEventListener('DOMContentLoaded', (event) => {
	//onces the images are loaded, then add listeners to the images
	//if the mouse enters an area or clicks an area, then trigger the function
	//that shows the toolsnip; how to figure out how long the mouse remains in that
	//area? 
})

